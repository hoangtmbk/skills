node_modules

# `npx skills` self-install: this repo's own skills copied back into it.
# Redundant here — scripts/link-skills.sh installs them globally instead.
.agents/skills/
.claude/skills/
skills-lock.json
